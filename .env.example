# Django
SECRET_KEY=change-me-to-a-random-string
DJANGO_SETTINGS_MODULE=config.settings.production

# Database (host "db" is the compose service name)
DATABASE_URL=postgres://limpid:change-me@db:5432/limpid

# Production
ALLOWED_HOSTS=limpid.viniqo.com,localhost
CSRF_TRUSTED_ORIGINS=https://limpid.viniqo.com

# PostgreSQL container
POSTGRES_DB=limpid
POSTGRES_USER=limpid
POSTGRES_PASSWORD=change-me

# Cloudflare Tunnel
TUNNEL_TOKEN=your-tunnel-token
